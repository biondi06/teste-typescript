{
  "name": "barbearia-agendamento",
  "version": "1.0.0",
  "description": "Site para agendamento de horários em uma barbearia",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && live-server public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4",
    "live-server": "^1.2.2"
  }
}
